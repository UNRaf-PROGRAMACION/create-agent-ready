# Security Policy

Do not report secrets in public issues. Use GitHub private reporting when it is available for this repository, or contact the maintainers through the institution's established channel.

The CLI never reads environment-variable values or sends project files over the network. The optional `--skills` integration explicitly invokes AutoSkills, which has its own registry, license, and security model.
