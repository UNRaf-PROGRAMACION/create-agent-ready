# Contributing

Keep the CLI safe for existing projects: inspect before writing, do not add network actions by default, and cover every generated artifact with tests.

Run `npm run check` before opening a pull request. Changes to generated files must also update the relevant documentation in `docs/`.
